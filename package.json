{
  "name": "cloudflare-yahoo-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Cloudflare Worker reverse proxy for Yahoo Finance (yfinance / Zeabur)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/worker.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  },
  "engines": {
    "node": ">=18"
  }
}
