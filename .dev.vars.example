# Copy to .dev.vars for local `wrangler dev` (never commit secrets).
PROXY_KEY=dev-local-proxy-key

# Optional: comma-separated allowlist. Leave unset to allow any host.
# ALLOWED_HOSTS=query1.finance.yahoo.com,query2.finance.yahoo.com
