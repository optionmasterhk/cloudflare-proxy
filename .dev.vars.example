# Copy to .dev.vars for local `wrangler dev` (never commit secrets).
PROXY_KEY=dev-local-proxy-key

# Optional override (comma-separated). Empty = built-in Yahoo allowlist.
# ALLOWED_HOSTS=query1.finance.yahoo.com,query2.finance.yahoo.com,fc.yahoo.com,finance.yahoo.com
